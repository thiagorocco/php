<?php 
    // Constante = variável que não pode ter seu valor modificado.
    //Usam as mesmas regras para nomenclatura das variáveis, porém não utilizam o modificador $ antes do nome.

    define('VALOR_MAX',100);

    echo(VALOR_MAX);
